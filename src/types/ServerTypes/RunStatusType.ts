export enum RunStatusType {
    IDLE = 'idle',
    RUNNING = 'running',
    PAUSED = 'paused',
    FINISHING = 'finishing',
    SUCCEEDED = 'succeeded',
    STOPPED = 'stopped',
    STOP_REQUESTED = 'stop-requested',
    UNKNOWN = 'UNKNOWN',
} 